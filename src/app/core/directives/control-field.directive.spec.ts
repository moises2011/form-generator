import { ControlFieldDirective } from './control-field.directive';

describe('ControlFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new ControlFieldDirective();
    expect(directive).toBeTruthy();
  });
});
